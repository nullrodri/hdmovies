import styled from "styled-components";

export const StyledRatingFilterWrapper = styled.div`
  font-size: 2em;
  display: flex;
  align-items: center;

  @media (max-width: 700px) {
    flex-direction: column;
  }
`;

export const StyledFilterWrapper = styled.div`
  margin: 0.5rem;
`;
